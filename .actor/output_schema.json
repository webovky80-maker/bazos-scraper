{
    "actorOutputSchemaVersion": 1,
    "title": "Bazoš scraper output",
    "description": "Scraped ads are stored as items in the Actor's default dataset.",
    "properties": {
        "ads": {
            "type": "string",
            "title": "Scraped ads",
            "description": "Ad listings scraped from Bazoš, one item per ad.",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}
